/// <reference path="./../masterScope.d.ts" />
'use strict';

angular.module('mobileMasterApp').controller('FiltersCtrl', (
	$rootScope:MasterScope.Root,
	$scope) => {

	/*$rootScope.$watch('types', () => {
		angular.forEach($rootScope.things, (thing: MasterScope.Thing) => {
			thing.visible = $rootScope.types[thing.typeName].visible;
		});

	},true);*/
  });
